[project]
name = "aiagent"
version = "0.1.0"
description = "A tool-using AI coding agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]
